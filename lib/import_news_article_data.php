<?php 
function insert_article_into_db($db, $article)
{
    error_log("article: " . var_export($article, true));
    // // Prepare SQL query
    // $query = "INSERT INTO `NewsArticles` ";
    // if(count($article) > 0){
    //     $cols = array_keys($article[0]);
    // }
}
?>